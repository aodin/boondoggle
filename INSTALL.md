INSTALL
====

Once [Go](https://go.dev) is installed, `cd` into the `/cmd/boondoggle` directory and run:

    go install

The `boondoggle` command should be installed to either your `GOBIN` or the `bin` directory of your `GOPATH`. Make sure this directory is in your `PATH` in order to use the command globally.
